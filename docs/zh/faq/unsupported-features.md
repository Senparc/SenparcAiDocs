# 尚未提供能力说明

在 `Senparc.AI.Samples.AgentKernelConsoles` 中，以下菜单项当前会提示“尚未提供”：

- Planner 任务计划
- PluginFromObject / Function Calling

这是当前 Sample 的已知状态，不是你的环境问题。

## 替代建议

- 对 AgentKernel 当前已稳定能力：优先使用 Chat/Completion/Embedding/RAG/Image/STT/TTS。
- `Senparc.AI.Kernel` 模块已停止维护，不建议作为新方案的替代路线。
